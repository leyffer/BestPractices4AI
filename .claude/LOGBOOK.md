# Logbook

A short summary of each working session, newest first. Each entry links to its detailed plan in
`plans/` where applicable.

## 2026-08-01 — Move bookkeeping files into `.claude/`

Moved `CLAUDE.md`, `LOGBOOK.md`, `STATUS.md`, and `plans/` from the repo root into `.claude/` to keep
the root tidy. Confirmed via the official docs that `.claude/CLAUDE.md` is still auto-loaded as
project memory. Verified `mkdocs build` output is unchanged (`.claude/` is outside `docs/`).

## 2026-08-01 — Set up CLAUDE.md and session protocol

Created `CLAUDE.md` documenting the MkDocs project (structure, local preview/build/deploy workflows)
and establishing the session bookkeeping file structure and protocol. Seeded `LOGBOOK.md`,
`STATUS.md`, and `plans/`.

- Plan: [PLAN-2026-08-01-Setup-Session-Protocol.md](plans/PLAN-2026-08-01-Setup-Session-Protocol.md)
